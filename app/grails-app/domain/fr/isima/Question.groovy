package fr.isima

class Question {

    String title

    // Many to many keywords
    // static hasMany = [ tags: Keyword[] ]

    // One to one Topic
    // static belongsTo = [ topic: Topic ]

    static constraints = {
    }
}
